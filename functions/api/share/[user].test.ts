import { describe, test } from "vitest";

describe("/api/share/:user", () => {
  // Need ability to work with r2 - https://github.com/cloudflare/miniflare/issues/600
  test.skip("/api/share/:user", () => {});
});
